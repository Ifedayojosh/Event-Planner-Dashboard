import React from "react";

export default function EventPlannerLayout ({children}:{
    children: React.ReactNode;
}) {
    return (
        <>
        {children}
        
        </>
        
    );
}
